# Configuraciones para administrar y configurar un backend de Firebase App Hosting.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Aumenta este valor si deseas activar automáticamente
  # más instancias en respuesta al aumento del tráfico.
  maxInstances: 1
